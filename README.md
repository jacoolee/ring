```
ring

web --> urlscheme --> scripts --> web/others

1. download and extract ./ring.app.zip, move extracted ring.app into /Applications;
2. cp './ring.sh' into /Applications, aka, '/Applications/ring.sh';
2. open /Applications/ring.app to register 'ring://' to OS;
3. run `open ring://whaterver` in Terminal or paste `ring://whaterver` into Web Browser Address bar;
4. modify '/Applications/ring.sh' to fit your need.
```
